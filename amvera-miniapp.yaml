meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  scriptName: timur_bot/web/admin_panel.py
  persistenceMount: /data
  containerPort: 80
