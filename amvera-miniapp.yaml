meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  command: python3 -m timur_bot.web.admin_panel
  persistenceMount: /data
  containerPort: 80

